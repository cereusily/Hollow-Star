class BossEnemy extends Enemy {
  // Fields for boss enemy
  
  BossEnemy(PVector pos, PVector vel, int health, int charWidth, int charHeight, float scaleFactor, String state, boolean isElite) {
    super(pos, vel, health, charWidth, charHeight, scaleFactor, state, isElite);
  }
  
  
}
